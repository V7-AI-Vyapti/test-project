import { Module } from '@nestjs/common';

/**
 * Ingest API modules register here via project generation codegen.
 * Extend `imports` when file-ingest or entity-ingest tools are configured.
 */
@Module({
    imports: [],
})
export class GeneratedIngestApiModule {}
